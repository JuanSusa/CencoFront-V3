import { FieldsOnlyNumbersDirective } from './fields-only-numbers.directive';

describe('FieldsOnlyNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldsOnlyNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
